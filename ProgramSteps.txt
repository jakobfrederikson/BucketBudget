BucketBudget
>> Ultimate goal:
> Create/download/upload style website. No user accounts.
> Create a budget.
> Download the budget as a JSON file.
> Upload the JSON file to edit your budget.

>> MVP - Finished.
> Create a budget.
> Income(s) (something/someone that earns money for the budget).
> Expense (something that takes money out of the budget).
> Frequency (is this a weekly, fortnightly, monthly or yearly budget).
> Bucket (Net income, after expenses, divided into buckets).
-> Buckets know to say '$X.XX into [X] Bucket per [Frequency]'.
-> Buckets always add to the net income.

Budgets can have:
- One frequency
- One owner
- One created date
- One title
- Many members
- Many incomes
- Many expenses
- Many buckets

Incomes can have:
- a title
- an amount
- a frequency
- the ability to distuinguish if the buckets should be split for this income

Expenses can have:
- a title
- an amount
- a frequency
- the ability to distuinguish if the expense is an important bill or not 

Users can have:
- One username
- One password
- Many budgets

>> To Dos:
> Done: Update form validation
    - Used WTForms
> Done: Add budget collaboration.
    - Add ability to invite users to a budget | DONE
    - Users must accept an invite first | DONE - just created an invite code for users
    - Once accepted, users can see this new budget on their 'budget dashboard' area? | DONE
    - Let budget owner edit budget members | DONE
    -- The only thing the owner can do is switch ownership from themself
    -- create a form that takes a list of all budget members
    -- exclude the current owner
    -- on submission, check if the submitted member id is the current users
    --- if yes then redirect back to form advising they can't do this
    -- check if the current user is the owner of the budget
    --- if not, then redirect back to form advising they don't have permission
    -- all good now, so do the following:
    ---> set owner_id on budget to member_id and commit the executed sql
    ---> redirect user back to the budget index advising ownership transfer successful
> Done: Security (was User update)
    - Use Flask-Security, may include using SQLAlchemy - DONE
    - User Email verification (Send register email, confirm email) - DONE
    - Block users from accessing budget pages if not a budget member - DONE
    - User password reset - DONE
    - Email and Authenticator for 2FA - SCRAPPED - no need for this, the app relies on user input without any integrations.
> Done: Form update
    - Add Delete forms with CSRF tokens for each table - DONE
> Done: Custom abort pages
    - Create custom abort pages, for when the site can't load a specific resource - DONE
> Done: Store 'amounts' as lowest possible units
    - Convert all amounts for income/expense item and bucket percent to integer - DONE
    - Change how the money is then stored and handled - DONE
> Doing: UI/UX Update
    - Make sure the 'amounts' are all rounded to 2 decimal points on UI - DONE
    - Add ability to go back to 'budget index' page on each budget form - DONE
    - Create base colour scheme - DONE (bulma)
    - Createa a better diagram showing how you use the app
    - Update base, consistent layout for forms - DONE
> Done: Migrate to PostgreSQL
> Done: Check if budget result is working properly
> Skipped: Generate UUIDs for Public IDs? - no need
> DONE: Budget Generation and Limitation
    - Generate a templated budget for the user once they're verified - DONE
    - Explain why this was generated, so that users know this is how the barefoot investor gets you to start budgeting - MEH
    - Limit budget creation to 2 per user - DONE
> DONE: Password complexity
    - Create password requirments
> DONE: Let Users leave a budget
> DONE: Add percentage bar to buckets section
    - Link percentage bar to current total percentage - DONE
    - Display percentages amount in the bar - DONE
> DOING: Allow users to change their email
    - Configure Flask-Security 'SECURITY_CHANGE_EMAIL' to true
    - Check if there is a default template required for bucket budget, if so, customise it
> To Do: Create tests
    - Test CRUD with all forms.
    - Test user creation and deletion.
    - Test budget collaboration.
    - Test password reset. 


Budget Collaboration:
- add invite_code stringfield to budget
- upon budget creation, generate a uuid for the invite_code

>> Nice to have(s)
> Expected savings. Show how much will be saved in each bucket in 6 months, then 1 year.
> Budget Check Ins. Save your current amount in each bucket. You can then view how well your savings have gone.
>? Budget goals.
>? Site keeps cookies about an ID associated with the session?
>? Knows to automatically load the last budget that was being created from the last visit?

>> Helpful references:
> Understanding the language around budgets: https://moneybucket.org/budgeting/budgeting-glossary/